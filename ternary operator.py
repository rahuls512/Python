#The ternary operator in Python is a shorthand way of writing an if-else statement in a single line of code.

# if-else statement:
x = 5
if x > 0:
    y = 'positive'
else:
    y = 'non-positive'

# written using the ternary operator as:
x = 5
y = 'positive' if x > 0 else 'non-positive'
